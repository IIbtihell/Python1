def saisir(N):
  while (10=<N=<20):
  N=int(input("donner N")


def remplir(T,N):
For i in range (N) :
     While(N%10=0) and (100>=N<1000)
         T[i]=int(input("Donner un élément")

def afficher (T,N):
For i in range (N) :
     print(T[i])

def trier(T,N):
    for i in range(N) :
        Posmax=i
        for j in range(i+1,N) :
            if T[j]>T[Posmax] :
                Posmax=j
        aux=T[i]
        T[i] = T[Posmax]
        T[Posmax]=aux


def trie element(T,N):
for i in range (N) :
     a=T[i]//100
     b=(T[i]%100)//10
     if (A<B)
       T[i]=b*100+a*10

def inverser(T,N):
    i=0
    j=n-1
    while(i<j):
        aux=t[i]
        t[i]=t[j]
        t[j]=aux
        i=i+1
        j=j-1

#Programme principal:
print(n)
T[i](remplir(T,N))
print(T,N))
trier(T,N)
trie element(T,N)
inverser(T,N)
